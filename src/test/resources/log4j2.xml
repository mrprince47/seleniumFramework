<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">

    <Appenders>

        <!-- ========== CLEAN CONSOLE OUTPUT ========== -->
        <Console name="Console" target="SYSTEM_OUT">
        <PatternLayout pattern="[%t] %d{HH:mm:ss} %-5p - %m%n"/>
    </Console>

        <!-- ========== FULL DEBUG FILE LOGS ========== -->
        <RollingFile name="File"
                     fileName="logs/framework.log"
                     filePattern="logs/framework-%d{yyyy-MM-dd}.log.gz">

            <!-- Detailed + precise format -->
            <PatternLayout pattern="[%d{yyyy-MM-dd HH:mm:ss}] %-5p %c:%L - %m%n"/>

            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="5MB"/>
            </Policies>

            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

    </Appenders>

    <Loggers>

        <!-- ========== STOP ALL TESTNG LOGS ========== -->
        <Logger name="org.testng" level="off" additivity="false"/>

        <!-- ========== STOP SELENIUM INTERNAL LOGS ========== -->
        <Logger name="org.openqa.selenium" level="error" additivity="false"/>

        <!-- ========== YOUR FRAMEWORK LOGS ========== -->
        <!-- Any class under package 'tests', 'base', 'utilities' -->
        <Logger name="tests" level="info" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
        </Logger>

        <Logger name="base" level="info" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
        </Logger>

        <Logger name="utilities" level="info" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
        </Logger>

        <!-- ========== ROOT LOGGER (fallback) ========== -->
        <!-- If something still logs, we show only WARN+ in console -->
        <Root level="warn">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
        </Root>

    </Loggers>

</Configuration>
